/* =====================================================================
   Fill these two in after you create the Supabase project.
   Supabase dashboard -> Project Settings -> API

   Both values are meant to be public. The anon key can only do what the
   database security rules allow, which is: read the price list, read the
   team, read opening hours, check availability, and make a booking.
   It cannot read a single customer record. Never put the SERVICE ROLE
   key in this file — that one bypasses everything.
   ===================================================================== */
window.CONFIG = {
  SUPABASE_URL: "https://YOUR-PROJECT.supabase.co",
  SUPABASE_ANON_KEY: "YOUR-ANON-KEY"
};
